// COMPOUND ASSIGNMENT -> BISA UNTUK OPERASI UNTUK DATA REAL & BIT
/*
    +=
    -+
    *=
    /=
    %=
    >>=
    <<=
    &=
    ^=
    |=
*/

/*
    misal int 5 -> 0101
    misal int 3 -> 0011
    jika di |=  -> 0111 -> hasil secara int jadi 7.

        a += 1;         // a = a + 1;
        a /= 5;         // a = a / 5;
        a *= b + c;     // a = a * (b + c);

        cost += items * tax; // cost = cost + (items * tax);

*/